@if (CurrentDateTime is not null)
{
    <MudText Typo="Typo.body1" Class="pl-5">
        It's currently <LocalTime DateTime="@CurrentDateTime.Value" /> 
    </MudText>
}

@code
{
    [Parameter]
    public DateTime? CurrentDateTime { get; set; }
}