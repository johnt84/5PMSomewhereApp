@if (TimeZone is not null)
{
    <MudText Typo="Typo.body1" Class="mt-5 mb-2 ml-5">
        It's currently 5 PM in <CountryChipComponent Country="@TimeZone.RandomCountry" />
    </MudText>

    <TimeZone Name="@TimeZone.TimeZoneName" />

    <CountriesComponent Countries="@TimeZone.Countries" />
}

@code{
    [Inject]
    private IFivePMSomewhereService FivePMSomewhereService { get; set; } = null!;
    
    [Parameter]
    public TargetTimeModel? TimeZone { get; set; }
}